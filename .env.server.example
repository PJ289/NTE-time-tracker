# Server settings (copy to .env.server for local node server.js, or use .env for Docker Compose)
NTE_HOST=0.0.0.0
NTE_PORT=28183
NTE_ADMIN_TOKEN=genera_un_token_largo_aqui

# Optional overrides
# NTE_MIN_SESSION_SECONDS=30
# NTE_MERGE_GAP_SECONDS=120
