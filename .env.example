# Copy to .env and edit before: docker compose up -d
# Not baked into the image — change anytime and restart the container.

NTE_HOST=0.0.0.0
NTE_PORT=28183
NTE_ADMIN_TOKEN=change_me_to_a_long_random_token

# Optional
# NTE_MIN_SESSION_SECONDS=30
# NTE_MERGE_GAP_SECONDS=120
# TZ=Europe/Madrid
